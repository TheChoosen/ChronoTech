/*!
FullCalendar Mock for ChronoTech Dashboard
*/
(function() {
    'use strict';
    
    // Mock FullCalendar object pour éviter les erreurs ReferenceError
    window.FullCalendar = {
        Calendar: function(element, options) {
            console.log('📅 FullCalendar Mock Calendar créé sur:', element);
            
            // Mock calendar object
            return {
                render: function() {
                    console.log('📅 FullCalendar Mock render() appelé');
                    if (element && typeof element.innerHTML !== 'undefined') {
                        element.innerHTML = '<div style="padding: 20px; background: #f8f9fa; border: 1px solid #dee2e6; border-radius: 5px; text-align: center;">' +
                                          '<h5>📅 Calendrier FullCalendar</h5>' +
                                          '<p class="text-muted">Version mock - Calendrier sera fonctionnel une fois FullCalendar chargé correctement</p>' +
                                          '</div>';
                    }
                },
                
                destroy: function() {
                    console.log('📅 FullCalendar Mock destroy() appelé');
                },
                
                refetchEvents: function() {
                    console.log('📅 FullCalendar Mock refetchEvents() appelé');
                },
                
                addEvent: function(event) {
                    console.log('📅 FullCalendar Mock addEvent() appelé:', event);
                },
                
                removeAllEvents: function() {
                    console.log('📅 FullCalendar Mock removeAllEvents() appelé');
                },
                
                gotoDate: function(date) {
                    console.log('📅 FullCalendar Mock gotoDate() appelé:', date);
                },
                
                getDate: function() {
                    return new Date();
                },
                
                next: function() {
                    console.log('📅 FullCalendar Mock next() appelé');
                },
                
                prev: function() {
                    console.log('📅 FullCalendar Mock prev() appelé');
                },
                
                today: function() {
                    console.log('📅 FullCalendar Mock today() appelé');
                }
            };
        },
        
        // Méthodes statiques
        formatDate: function(date, format) {
            return date.toISOString().split('T')[0];
        }
    };
    
    console.log('✅ FullCalendar Mock initialisé - Les erreurs ReferenceError sont résolues');
    
})();
